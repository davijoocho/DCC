
instructn sub (int a, int b) => int {
    return a - b;
}

instructn add (int x, int y) => int {
    return x + y;
}

instructn main() => void {
    int c = sub(10, 2);
    int d = add(c, 2);
    output(c);
    output(d);
}
